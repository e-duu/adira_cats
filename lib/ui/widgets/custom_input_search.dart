import 'package:flutter/material.dart';

class CustomInputSearch extends StatelessWidget {
  const CustomInputSearch({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}